"use client";import {FormEvent,useState} from "react";export default function Login(){const[e,setE]=useState("");const[l,setL]=useState(false);async function go(x:FormEvent<HTMLFormElement>){x.preventDefault();setL(true);setE("");const f=new FormData(x.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(r.ok)location.href="/dashboard";else{setE("Invalid email or password.");setL(false)}}return <main className="shell" style={{maxWidth:500,paddingTop:90}}><form className="card" onSubmit={go}><h1>Sign in</h1><label>Email<input className="input" name="email" type="email" required/></label><label>Password<input className="input" name="password" type="password" required/></label>{e&&<p className="danger" role="alert">{e}</p>}<button className="btn" disabled={l}>{l?"Signing in…":"Sign in"}</button><p className="muted">Demo: student1@bythursday.demo / Demo12345!</p></form></main>}