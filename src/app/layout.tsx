import type {Metadata} from "next";import "./globals.css";export const metadata:Metadata={title:"byThursday Tutoring Platform",description:"Secure tutoring quizzes"};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en" dir="auto"><body>{children}</body></html>}