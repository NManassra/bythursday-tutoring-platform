"use client";import {useEffect,useState} from "react";export default function TeacherDashboard({user}:{user:{name:string}}){const[q,setQ]=useState<any[]>([]);const[classes,setClasses]=useState<any[]>([]);const[title,setTitle]=useState("");const[classId,setClassId]=useState("");useEffect(()=>{Promise.all([fetch("/api/quizzes"),fetch("/api/classes")]).then(async([a,b])=>{setQ((await a.json()).quizzes??[]);setClasses((await b.json()).classes??[])})},[]);async function create(){const now=new Date();const body={title,description:"Created from teacher dashboard",opensAt:now.toISOString(),closesAt:new Date(now.getTime()+86400000).toISOString(),durationMinutes:20,pointsPerQuestion:1,negativeMarkPercent:0,classId,questions:[{text:"Sample question",options:[{text:"Correct",isCorrect:true},{text:"Incorrect",isCorrect:false},{text:"Incorrect",isCorrect:false},{text:"Incorrect",isCorrect:false}]}]};const r=await fetch("/api/quizzes/create",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(r.ok)location.reload()}return <main className="shell"><div className="top"><div><p className="muted">Teacher</p><h1>{user.name}</h1></div><button className="btn secondary" onClick={async()=>{await fetch("/api/auth/logout",{method:"POST"});location.href="/login"}}>Sign out</button></div><div className="card"><h2>Create quiz</h2><div className="row"><input className="input" style={{maxWidth:360}} placeholder="Quiz title" value={title} onChange={e=>setTitle(e.target.value)}/><select className="input" style={{maxWidth:260}} value={classId} onChange={e=>setClassId(e.target.value)}><option value="">Select class</option>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button className="btn" disabled={!title||!classId} onClick={create}>Create draft</button></div></div><div className="card" style={{marginTop:16}}><h2>Quizzes</h2>{q.map(x=><div key={x.id} style={{padding:"14px 0",borderBottom:"1px solid #e2e8f0"}}><b>{x.title}</b><span className="muted"> · {x.status} · {x.attempts.length} attempts</span></div>)}</div></main>}