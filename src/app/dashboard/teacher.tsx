"use client";import {useEffect,useState} from "react";export default function TeacherDashboard({user}:{user:{name:string}}){const[q,setQ]=useState<any[]>([]);useEffect(()=>{fetch("/api/quizzes").then(r=>r.json()).then(x=>setQ(x.quizzes??[]))},[]);return <main className="shell"><div className="top"><div><p className="muted">Teacher</p><h1>{user.name}</h1></div><button className="btn secondary" onClick={async()=>{await fetch("/api/auth/logout",{method:"POST"});location.href="/login"}}>Sign out</button></div><div className="card"><h2>Quizzes</h2>{q.map(x=><div key={x.id} style={{padding:"14px 0",borderBottom:"1px solid #e2e8f0"}}><b>{x.title}</b><span className="muted"> · {x.status} · {x.attempts.length} attempts</span></div>)}</div></main>}