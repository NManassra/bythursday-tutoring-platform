import {redirect} from "next/navigation";import {getCurrentUser} from "@/lib/session";import StudentDashboard from "./student";import TeacherDashboard from "./teacher";export default async function Dashboard(){const u=await getCurrentUser();if(!u)redirect("/login");return u.role==="STUDENT"?<StudentDashboard user={u}/>:<TeacherDashboard user={u}/>}